type OptionType = {
  color?: {
    backgroundColor?: string,
    borderColor?: string,
    headTitle?: string,
    headTitleHover?: string,
    description?: string,
    itemTitle?: string,
    itemTitleHover?: string,
  },
  fullWidth?: boolean,
  image?: React.ReactNode,
  CTA?: React.ReactNode,
  responsive?: boolean,
}

export type {
  OptionType
}