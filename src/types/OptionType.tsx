type OptionType = {
  color?: {
    color?: string,
    backgroundColor?: string,
    borderColor?: string,
  },
  fullWidth: boolean,
  image?: React.ReactNode,
  CTA?: React.ReactNode,
  responsive?: boolean,
}

export type {
  OptionType
}