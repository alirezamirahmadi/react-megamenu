import { OptionType } from "../types/OptionType";

const defaultOptions: OptionType = {
  color: {
    color: '#000',
    backgroundColor: '#fff',
    borderColor: '#ddd'
  },
  fullWidth: false,
  responsive: true,
}

export default defaultOptions;